# `packages/templates`

業種ごとの `preset.json`（`SiteConfig` 相当）を置く。新規デモアプリを作るときは `apps/<name>/data/site.json` にコピーして店名・連絡先だけ差し替える。

- `restaurant/preset.json` — 焼き鳥デモ相当
- `cafe/preset.json` — カフェ文面サンプル
- `salon/preset.json` — サロン文面サンプル
